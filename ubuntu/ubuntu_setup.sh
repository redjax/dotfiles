#!/bin/bash

# Setup script for Ubuntu installs

# Install snap store
snap install snap-store

# Install snaps
snap install gimp
snap install youtube-dl
snap install spotify
snap install ufw
snap install vlc
snap install syncthing
snap install htop
snap install gnome-system-monitor
snap install discord
snap install wire
snap install code --classic

