## Only set the prompt if it hasn't been set already
if [[ -z "$BASE_PROMPT_SET" ]]; then
  export PS1="%n@%m:%~ %# "
  export BASE_PROMPT_SET=1
fi

## Load powerlevel10k if available
if [[ -f ~/.p10k.zsh ]]; then
  source ~/.p10k.zsh
fi
