## Enable status bar
set -g status on

## Position in window for status bar
set -g status-position bottom

## Interval in seconds to update status bar
set -g status-interval 5

## Set status bar theme
# set -g status-style fg=white,bg=black
set -g status-bg black
# set -g status-fg white

# Format for inactive windows
set -g window-status-format "#[fg=cyan] #W#[default] "

# Format for the active (current) window
set -g window-status-current-format "#[fg=white,bg=blue] Window: #W #[default]"

## stock/no-plugins status bar
# if-shell '[ -f {{ .chezmoi.homeDir }}/.config/tmux/statusbar.stock.conf ]' 'source-file {{ .chezmoi.homeDir }}/.config/tmux/statusbar.stock.conf'

## monochrome status bar
if-shell '[ -f {{ .chezmoi.homeDir }}/.config/tmux/statusbar.monochrome.conf ]' 'source-file {{ .chezmoi.homeDir }}/.config/tmux/statusbar.monochrome.conf'
